# Three-Level Orchestration

Explicit-only skill for supervised three-level delegation on Orca: a root team leader, review leaders, and implementation workers sharing one worktree.

## Install

```sh
npx skills add serialgeronimo/three-level-orchestration -g -y
```

Invoke it with `$three-level-orchestration`.
